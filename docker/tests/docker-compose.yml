version: "3.7"

services:
  web:
    # Overrides web.command from the original docker-compose file
    command: bash -c 'while !</dev/tcp/postgres/5432; do sleep 1; done; coverage run --source='.' manage.py test; coverage report; coverage html'
